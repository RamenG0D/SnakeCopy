
# Snake

I have changed the code base to use rust, I might try to use java again or I might not (prob not).

Or another fun idea might be writing it using both with robusta, but thats probably for another day.
